# Domain Docs Layout

Single-context layout.

- `CONTEXT.md` — domain glossary at repo root (已创建)
- `docs/adr/` — architecture decision records（目录已初始化，可在此添加 `.md` 记录）

Skills read `CONTEXT.md` for domain language and `docs/adr/*.md` for past architectural decisions.
