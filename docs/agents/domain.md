# Domain Docs Layout

Single-context layout.

- `CONTEXT.md` — domain glossary at repo root
- `docs/adr/` — architecture decision records

Skills read `CONTEXT.md` for domain language and `docs/adr/*.md` for past architectural decisions.